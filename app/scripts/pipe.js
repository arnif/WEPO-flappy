window.Pipe = (function() {
    'use strict';

    var Pipe = function(el, game) {
        this.el = el;
        this.game = game;
        this.pos = { x: 0, y: 0 };
    };

    console.log(Pipe);


    return Pipe;

})();
